import Image from 'next/image';import Link from 'next/link';import type {Anime} from '../lib/jikan';import {poster} from '../lib/jikan';
export function AnimeCard({anime}:{anime:Anime}){return <Link className="animeCard" href={`/anime/${anime.mal_id}`}><div className="poster"><Image src={poster(anime)} alt={anime.title} fill sizes="(max-width:700px) 42vw, 180px"/><span className="score">★ {anime.score??'—'}</span>{anime.episodes&&<span className="epBadge">{anime.episodes} EP</span>}</div><div className="cardTitle">{anime.title}</div><div className="cardMeta">{anime.type||'Anime'} · {anime.year||'—'}</div></Link>}
