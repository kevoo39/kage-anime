import {supabaseBrowser} from './supabase-browser';
export async function upsertProgress(input:{profile_id:string;mal_id:number;episode:number;audio:'sub'|'dub';position_seconds:number;duration_seconds:number}){const sb=supabaseBrowser();if(!sb)return {error:new Error('Supabase is not configured')};const{data:{user}}=await sb.auth.getUser();if(!user)return {error:new Error('Not signed in')};return sb.from('continue_watching').upsert({...input,user_id:user.id,updated_at:new Date().toISOString()},{onConflict:'profile_id,mal_id,episode,audio'}).select().single()}
export async function listContinueWatching(profileId:string){const sb=supabaseBrowser();if(!sb)return {data:[],error:null};return sb.from('continue_watching').select('*').eq('profile_id',profileId).order('updated_at',{ascending:false})}
export async function removeContinueWatching(id:string){const sb=supabaseBrowser();if(!sb)return{error:new Error('Supabase is not configured')};return sb.from('continue_watching').delete().eq('id',id)}
