import type { Metadata, Viewport } from 'next';import './globals.css';import {BottomNav} from '../components/bottom-nav';import {TopNav} from '../components/top-nav';
export const metadata: Metadata={title:'KAGE — Anime',description:'KAGE is a premium anime discovery and streaming experience with personalized profiles, watch progress, saved titles and authorized playback.',manifest:'/manifest.webmanifest',icons:{icon:'/icon-512.svg',apple:'/icon-512.svg'},openGraph:{title:'KAGE — Anime',description:'Premium anime discovery and streaming experience.'},twitter:{card:'summary_large_image',title:'KAGE — Anime',description:'Premium anime discovery and streaming experience.'}};
export const viewport:Viewport={themeColor:'#0f0a14',colorScheme:'dark'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body><div className="appShell"><TopNav/>{children}<BottomNav/></div><script dangerouslySetInnerHTML={{__html:`if('serviceWorker' in navigator && location.hostname!=='localhost'){window.addEventListener('load',()=>navigator.serviceWorker.register('/sw.js').catch(()=>{}))}`}}/></body></html>}
