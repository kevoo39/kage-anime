import {jikan} from '../../../../lib/jikan';import {LicensedPlayer} from '../../../../components/licensed-player';
export default async function Watch({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{episode?:string}>}){const{id}=await params;const{episode='1'}=await searchParams;const a=await jikan<any>(`/anime/${id}`).catch(()=>null);return <main className="section"><div className="eyebrow">Now Playing</div><h1>{a?.title||'Anime'}</h1><div className="videoFrame"><LicensedPlayer title={a?.title||'Anime'} episode={Number(episode)||1}/></div><p className="muted">Playback accepts a licensed HLS/MP4 source configured by the application owner. KevStream does not extract or proxy unauthorized streams.</p></main>}
