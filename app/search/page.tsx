'use client';
import {useState} from 'react';import {AnimeCard} from '../../components/anime-card';import type {Anime} from '../../lib/jikan';
export default function Search(){const[q,setQ]=useState('');const[data,setData]=useState<Anime[]>([]);const[busy,setBusy]=useState(false);const[error,setError]=useState('');async function run(e:React.FormEvent){e.preventDefault();if(!q.trim())return;setBusy(true);setError('');try{const r=await fetch('/api/jikan/anime?q='+encodeURIComponent(q.trim())+'&limit=24');const x=await r.json();if(!r.ok)throw Error(x.error);setData(x.data||[])}catch(e){setError(e instanceof Error?e.message:'Search unavailable.')}finally{setBusy(false)}}return <main className="section searchPanel"><div className="eyebrow">AniDex Search</div><h1>Find your next anime</h1><form className="searchForm" onSubmit={run}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search titles…"/><button className="primary">Search</button></form>{busy&&<div className="empty">Searching…</div>}{error&&<div className="errorBox">{error}<br/><button className="secondary" onClick={()=>run(new Event('submit') as never)}>Retry</button></div>}{!busy&&!error&&data.length===0&&<div className="empty">Search the AniDex index to get started.</div>}{data.length>0&&<div className="grid" style={{marginTop:22}}>{data.map(a=><AnimeCard key={a.mal_id} anime={a}/>)}</div>}</main>}
